name: "${model.ssl_method}_${model.net}_pretrained_${model.pretrained}"
net: Resnet
pretrained: True
ssl_checkpoint_available: False
ft_checkpoint_available: False
trained_model_path: weights/ssl_ukb_weights.mdl  # Pre-trained SSL weights from OxWearables
name_start_idx: 1
